<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Project_Akhir_Mobile"
        tools:targetApi="31">

        <activity
            android:name="com.example.project_akhir_mobile.Navigasi"
            android:exported="false"
            android:label="@string/title_activity_navigasi"
            android:theme="@style/Theme.Project_Akhir_Mobile.NoActionBar" />
        <!--
             TODO: Before you run your application, you need a Google Maps API key.

             To get one, follow the directions here:

                https://developers.google.com/maps/documentation/android-sdk/get-api-key

             Once you have your API key (it starts with "AIza"), define a new property in your
             project's local.properties file (e.g. MAPS_API_KEY=Aiza...), and replace the
             "YOUR_API_KEY" string in this file with "${MAPS_API_KEY}".
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAJp_VsJ_7FvG360QYJ3TyhKQkzSsjWImM" />

        <activity
            android:name="com.example.project_akhir_mobile.Maps"
            android:exported="false"
            android:label="@string/title_activity_maps" />
        <activity
            android:name=".SplashScreenActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>
        <activity android:name=".view.detail_hotel" />
        <activity android:name=".view.detail_kuliner" />
        <activity android:name=".view.detail_wisata" />
        <activity android:name=".SecondActivity" />
        <activity android:name=".home" />
        <activity android:name=".activity_register" />
        <activity android:name=".onboarding_satu" />
        <activity android:name=".onboarding_dua" />
        <activity android:name=".onboarding_tiga" />
        <activity android:name=".profil" />
        <activity android:name=".activity_login" />
        <activity android:name=".view.MainWisata" />
        <activity android:name=".view.MainKuliner" />
        <activity android:name=".view.MainActivity" />
        <activity android:name=".loginggogle" />
    </application>

</manifest>